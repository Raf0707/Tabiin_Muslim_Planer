package com.example.tabiin.adapters;

import android.view.LayoutInflater;
import androidx.recyclerview.widget.RecyclerView;
import android.content.Context;
import android.view.ViewGroup;
import android.view.View;
import android.widget.TextView;

import java.util.*;

public class SavesCounterAdapter {// extends RecyclerView.Adapter<SavesCounterAdapter.ViewHolder> {

    /*

    private LayoutInflater inflater;
    private List<Counter> counters;
    private TextView name;

    public SavesCounterAdapter(Context context, List<Counter> counter) {
        this.counters = counter;
        this.inflater = LayoutInflater.from(context);
    }
    @Override
    public SavesCounterAdapter.ViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {

        View view = inflater.inflate(R.layout.fragment_counter_saves, parent, false);
        return new ViewHolder(view);
    }



    @Override
    public void onBindViewHolder(final SavesCounterAdapter.ViewHolder holder, int position) {
        Counter counter = counters.get(position);
        holder.nameView.setText(name.getCounter());
        holder.nameView.setOnClickListener(new View.OnClickListener(){
            @Override
            public void onClick(View v){

            }
        });
    }

    @Override
    public int getItemCount() {
        return counters.size();
    }

    public class ViewHolder extends RecyclerView.ViewHolder {
        final TextView nameView;
        ViewHolder(View view){
            super(view);
            nameView = (TextView) view.findViewById(R.id.name);
        }
    }


*/

}



